::: screenshot
src: assets/07-audience-mode.png
alt: 外部画面に全体表示された聴衆向けスライド。黒い余白の中央にスライドだけが収まり、操作ボタンや発表者ノートは表示されていない。
badge: 2画面で実画面確認済み
caption: 上の **発表** を押すと、2画面以上では外部画面に聴衆向けスライドだけを表示します。操作列や選択表示は重ならず、スライド内のスクロールバーも表示されません。
:::

::: steps
1. 発表前にノート、スライド順、**確認** の指摘を見直します。
2. 上の **発表** を押します。
3. 2画面以上では、主画面に出る発表者ウィンドウからすべて操作します。外部画面には操作列を出しません。
4. 1画面ではpointerを動かすと操作列が一時表示され、操作を止めると自動で隠れます。`→`、`PageDown`、Spaceで次へ、`←`、`PageUp`で前へ進めます。
5. 画面を指すときは初期状態の **レーザー** を使います。clickで点、dragで線を描き、約2秒で消えます。
6. 残して説明するときは **ペン** に切り替えます。赤、黄、青、黒、白から色を選べ、閉じた線も塗りつぶされません。線は **描画を消去** を押すまで残ります。
7. 終了するときは `Esc` または **編集へ戻る** を押します。
:::

2画面以上では、外部画面に聴衆向けスライド、主画面に発表者ウィンドウを出します。発表者ウィンドウは、左がスライド一覧、中央が現在のスライド、右が次のスライドと発表者ノートです。3画面以上では、主画面以外のうち最も広い画面を投影先に選びます。

::: screenshot
src: assets/08-presenter-window.png
alt: 発表者ウィンドウ。左にスライド一覧、中央に青いレーザーで指している現在のスライドと描画ツール・色選択、右に次のスライドと編集できる発表者ノートが表示されている。
badge: 実UI描画確認済み
caption: 2画面時に主画面へ出す **発表者ウィンドウ** です。現在と次のスライドは枠内へ全体表示され、内部スクロールバーは出ません。聴衆にはこの画面ではなく、外部画面のスライドだけを見せます。
:::

::: steps
1. 左のスライド一覧または中央上部の前後ボタンで、見せたいスライドへ移動します。
2. 中央の **現在のスライド** と、右上の **次のスライド** を見ながら進行します。
3. 右下の **発表者ノート** は発表中も直接修正できます。入力してもスライド映像は再読込されません。入力欄を選択中は、Spaceや矢印キーを文字入力に使えます。
4. 中央上部で **レーザー**／**ペン**、赤／黄／青／黒／白、**描画を消去** を操作します。レーザーは約2秒で消え、ペンは消去まで残ります。初期状態は赤いレーザーです。
5. 終了するときは右上の **終了** を押します。
:::

::: notice
発表者ノートの修正は編集画面へ戻った時点で未保存になります。発表終了後に上部の **保存** を押すと、同じHTMLへ上書き保存されます。レーザーとペンの描画は発表中だけの一時表示で、HTMLには保存されません。
:::

::: notice
掲載画像は、個人情報を含まない合成スライドを実アプリで開いて取得しています。発表者UI、画面選択、終了時の復元は自動テスト済みです。物理2画面への同時配置と、実際の会場機材を使う最終確認は利用者側で行ってください。
:::
